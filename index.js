// test 1-4: confirming that add, subtract, multiply, and divide are valid functions

function add() {};

function subtract() {};

function multiply() {};

function divide() {};

// test 5-8: basic ADDition, SUBTRACTion, MULTIPL(Y)ication, and DIVID(E)sion using their respective functions :T

function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}

// test 9-10: showing basic understanding of increment and decrement via implementation

function increment(a) {
    return a + 1;
}

function decrement(a) {
    return a - 1;
}

// test 11: parsed integers


function makeInt(a) {
    return parseInt(a);
}

// test 12, 13:  parsed integers with base of 10, return NaN. technically satisfies 11, 12, 13. 

function makeInt(a) {
    return parseInt(a, 10);
}

// test 14-15: parseFloat (decimal stuff)

function preserveDecimal(n) {
    return parseFloat(n);
}l

